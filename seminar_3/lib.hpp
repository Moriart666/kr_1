#pragma once
#include <iostream>
#include <vector>
#include <string>
#include <deque>
#include "gtest/gtest.h"

bool is_Subsequence(std::string a, std::string b);

bool is_Palindrome_1(std::string str);

bool is_Palindrome_2(std::string str);

bool is_Palindrome_3(std::string str);

int binary_search_sqrt(int number);

int copy_time(int n, int v1, int v2);

void delete_dublicat(std::string& array);